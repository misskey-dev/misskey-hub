import{_ as e,o as r,c as a,e as s}from"./app.b215b85c.js";const t={},n=s(`<h1 id="create-misskey-instance-with-kubernetes-truenas" tabindex="-1"><a class="header-anchor" href="#create-misskey-instance-with-kubernetes-truenas" aria-hidden="true">#</a> Create Misskey Instance with Kubernetes/TrueNAS</h1><p>This guide describes how to install and setup Misskey using Kubernetes and Helm charts.</p><h2 id="truecharts-and-truenas-scale" tabindex="-1"><a class="header-anchor" href="#truecharts-and-truenas-scale" aria-hidden="true">#</a> TrueCharts and TrueNAS Scale</h2><p>The Helm chart for Misskey is hosted on TrueCharts which is a repository designed to be used with TrueNAS Scale, but its charts can also be installed as normal Helm charts. The <a href="https://truecharts.org/docs/charts/incubator/misskey" target="_blank" rel="noopener noreferrer">TrueCharts</a> site has a list of all the charts available as well as documentation on how to install. The TrueCharts <a href="https://discord.gg/Ax9ZgzKx9t" target="_blank" rel="noopener noreferrer">Discord</a> server is also a resource that can be used if you have questions. Misskey is currently on the <em>incubator</em> train.</p><p>::: Requirements TrueNAS Scale or Kubernetes cluster &amp; Helm :::</p><h2 id="truenas-scale" tabindex="-1"><a class="header-anchor" href="#truenas-scale" aria-hidden="true">#</a> TrueNAS Scale</h2><p>Follow the instructions on the <a href="https://truecharts.org/docs/manual/guides/Adding-TrueCharts" target="_blank" rel="noopener noreferrer">TrueCharts Guide</a>. Add the <em>incubator</em> train, and install the Misskey app. The only Misskey configuration option that is required is the instance URL. The app also uses the Traefik reverse proxy to expose the service to the outside world over HTTPS. TrueCharts integrates with Traefik by default, but other options can be used with manual configuration.</p><h2 id="manual-helm" tabindex="-1"><a class="header-anchor" href="#manual-helm" aria-hidden="true">#</a> Manual Helm</h2><p>If you are not using TrueNAS Scale, you can install Misskey using Helm directly. The <code>misskey</code> object in the values.yaml file has the options that you will want to override as necessary. The <code>misskey.url</code> property is the only required change. A reverse proxy is recommended to secure access to the server as the chart does not currently support enabling TLS inside the Misskey container.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>helm repo add TrueCharts https://charts.truecharts.org
helm repo update
helm install misskey TrueCharts/misskey
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="upgrade-misskey" tabindex="-1"><a class="header-anchor" href="#upgrade-misskey" aria-hidden="true">#</a> Upgrade Misskey</h2><p>To upgrade Misskey, use the upgrade feature built into TrueNAS Scale or if running kubernetes manually use <a href="https://helm.sh/docs/helm/helm_repo_update/" target="_blank" rel="noopener noreferrer">helm repo update</a> and <a href="https://helm.sh/docs/helm/helm_upgrade/" target="_blank" rel="noopener noreferrer">helm upgrade</a>. It is highly recommended to create a snapshot of your data before upgrading in case there is an issue and you need to rollback.</p>`,12),i=[n];function o(h,l){return r(),a("div",null,i)}var d=e(t,[["render",o],["__file","kubernetes.html.vue"]]);export{d as default};
